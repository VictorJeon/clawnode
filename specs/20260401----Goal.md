## Goal
OpenClaw onboard starting

Original request: Fix the ClawNode macOS V4 installer issues.

## Goal
Make the real V4 installer flow work correctly for:
1) OpenClaw onboard starting
2) qwen3:4b install timing/gating
3) Gemini image/media key prompt collection

## Scope
Inspect and patch these exact files/functions/branches:
- `/Users/nova/projects/clawnode/installer/OpenClaw-Install-Mac.command`
  - confirm whether this wrapper points to the correct V4 gist/script artifact
- `/Users/nova/projects/clawnode/installer/scripts/openclaw-setup.sh`
  - inspect onboard branch around the auth/onboard section (roughly lines 399-481 in current file)
- `/tmp/openclaw-setup-v4.sh`
  - inspect and patch these exact functions if they are the real/current V4 source:
    - `core_install_present()`
    - `run_core_setup()`
    - `ensure_ollama()`
    - `configure_optional_google_api_key()`
    - any wrapper/update path that still points to the wrong raw gist filename

## Known observations you should start from
- Repo root wrapper currently downloads `raw/openclaw-setup.sh`, not an explicit `openclaw-setup-v4.sh` filename.
- In `/tmp/openclaw-setup-v4.sh`, `ensure_ollama()` currently pulls both the embedding model and `qwen3:4b` unconditionally.
- In `/tmp/openclaw-setup-v4.sh`, `configure_optional_google_api_key()` currently just preserves existing key or says post-wizard can set it later, which likely explains why no prompt happens.
- In `/Users/nova/projects/clawnode/installer/scripts/openclaw-setup.sh`, the Claude subscription/setup-token onboard branch uses a token path that may be wrong and should be verified against actual reachable onboard logic.

## Requirements
- Determine the real source-of-truth for V4. If ambiguous, patch BOTH:
  - repo-side source(s)
  - `/tmp/openclaw-setup-v4.sh`
- Fix onboard so the correct onboarding path is actually reachable and starts in the intended install flow.
- Fix qwen3:4b so it is only installed after the correct enrichment backend decision and only when that backend requires it.
- Fix Gemini key prompt so missing key is actually prompted/collected for image generation/media understanding, while still keeping the policy that Gemini key is NOT required for enrichment when OpenRouter/Ollama is selected.
- Keep non-technical UX simple.
- Do not regress prior enrichment-backend fixes.

## Acceptance Criteria
- shell syntax check passes on every modified script
- report exactly which file is the effective V4 source-of-truth now
- show the exact onboard branch/function that now runs and why
- show the exact qwen3:4b gate after the fix
- show the exact Gemini key prompt branch after the fix
- summarize follow-up needed for gist publishing/wrapper sync

## Notes
- Commit changes if the canonical source is inside the repo.
- If `/tmp/openclaw-setup-v4.sh` is also patched, mention it explicitly in the final result.

## Scope
- Working directory: `/Users/nova/projects/clawnode`
- 수정: OpenClaw onboard starting
- 금지: Do not change unrelated legacy registration, gating, or harness architecture outside this task.

## Acceptance Criteria
- OpenClaw onboard starting

## Context
- Harness plan: plan-20260401-kdxzsw
- Task id: task-1
- Plan mode: sequential
- Estimated complexity: high

---
## 작업 완료 후 필수
- CLAUDE.md가 있으면 이번 작업에서 배운 패턴/규칙/주의사항을 추가할 것
- CLAUDE.md가 없으면 새로 생성할 것
